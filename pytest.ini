[pytest]
# pytest-homeassistant-custom-component installs autouse async fixtures, which
# pytest only wires into sync tests when pytest-asyncio runs in auto mode.
# Without this every test errors out with PytestRemovedIn9Warning.
asyncio_mode = auto
